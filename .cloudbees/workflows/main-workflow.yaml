apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: test-bad-action
on:
  push:
    branches:
      - main
jobs:
  build:
    steps:
      - name: bad-action
        uses: cloudbees-io/totally-fake-action@v999