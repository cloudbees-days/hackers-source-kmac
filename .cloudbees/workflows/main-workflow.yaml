apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: test-invalid
on:
  push:
    branches:
      - main
jobs:
  build:
    steps:
      - name: missing-uses-step
        with:
          foo: bar
